---
- name: Meta | Converge
  hosts: all
  tasks:

    - name: "Molecule meta | Include variable file"
      ansible.builtin.include_vars: variables.yml

    - name: "Molecule meta | Include spreadcat.fule"
      ansible.builtin.include_role:
        name: "spreadcat.fule"
